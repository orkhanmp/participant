@using Entities.TableModels
@model Participant

<h2>Edit Participant</h2>

<form asp-controller="Participant" asp-action="Edit" method="post">
    <input type="hidden" asp-for="Id" />

    <div class="form-group">
        <label>Name</label>
        <input type="text" asp-for="Name" class="form-control" required />
    </div>

    <div class="form-group">
        <label>Age</label>
        <input type="number" asp-for="Age" class="form-control" required />
    </div>

    <div class="form-group">
        <label>Country</label>
        <input type="text" asp-for="Country" class="form-control" required />
    </div>

    <div class="form-group">
        <label>Team</label>
        <select asp-for="TeamId" class="form-control" required>
            <option value="">-- Select Team --</option>
            @if (ViewBag.Teams != null)
            {
                @foreach (var team in ViewBag.Teams as List<Team>)
                {
                    <option value="@team.Id">@team.TeamName</option>
                }
            }
        </select>
    </div>
    <br />

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-controller="Participant" asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>